import 'package:flutter/material.dart';

class ShareBookApp extends StatelessWidget {
  const ShareBookApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
